# Seguimiento del proyecto

## Proyecto

- Aplicación Next.js con TypeScript, Tailwind CSS y shadcn/ui.
- La página principal está en `app/page.tsx`.
- Fuente principal: Pinyon Script (Google Fonts).

## Estado actual de la interfaz

- Hero: ocupa `100vw` y `100vh`, con un mínimo de `450px` de alto.
- Fondo del hero: `hsl(188.7 94.5% 42.7%)`.
- Contenedor interior del hero: centrado horizontalmente, ancho máximo de `1200px`, alto completo y fondo `hsl(292 91.4% 72.5%)`.
- Segunda sección: situada debajo del hero, ocupa `100vw` y `50vh`, con un mínimo de `450px` de alto.
- Fondo de la segunda sección: `hsl(0 93.5% 81.8%)`.

## Desarrollo local

```cmd
cd /d D:\Dev.web.cl\15-09-2026\Primera_prueba
npm run dev
```

- Abrir la dirección local que muestre Next.js (normalmente `http://localhost:3000`).
- Detener el servidor con `Ctrl + C` en la terminal.
